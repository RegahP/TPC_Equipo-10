function drawIntro() {
    textAlign(CENTER, CENTER);
    text('intro', width / 2, height / 2);
}

function drawEndCombat() {
    textAlign(CENTER, CENTER);
    text('combat or town?', width / 2, height / 2);
}

function drawRest() {
    textAlign(CENTER, CENTER);
    text('resting', width / 2, height / 2);
}